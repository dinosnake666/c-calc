#include <unistd.h>
#include <stdlib.h>

void	ft_putnbr(int n);
int	ft_atoi(char *str);
void	ft_putstr(char *str);

int	main(int argc, char **argv)
{
	if (argc == 4)
	{

	}
	else
		ft_putstr("\aError : format not respected\tFormat : NUM1 OPERAND NUM2\n");
	retrun (0);
}
